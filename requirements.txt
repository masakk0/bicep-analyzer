# requirements.txt
# ─────────────────────────────────────────────
# Core
litellm>=1.80.0
networkx>=3.3
pyyaml>=6.0

# API server
flask>=3.0
flask-cors>=4.0
python-dotenv>=1.0

# UI (opzionale, solo se si usa Streamlit legacy)
# streamlit>=1.35.0

# Dev / test
pytest>=8.0
pytest-cov>=5.0